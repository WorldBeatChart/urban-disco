name: world_beat_chart
description: Global Music Charts
# Promenili smo verziju sa 1.0.0 na 1.0.1. 
# Ovo tera GitHub i browser da osveže keš memoriju.
version: 1.0.1+2

publish_to: 'none'

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  google_fonts: ^6.1.0
  flutter_animate: ^4.5.0
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
